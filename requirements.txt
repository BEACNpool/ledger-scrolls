# Currently no external PyPI dependencies required
# But good to have for future:
# oura              # (not on pypi – installed via cargo/rust)
# hailo-platform    # when you integrate real Hailo SDK
